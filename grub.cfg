# GRUB Configuration for OpenOS
# Sets boot timeout and default menu entry

set timeout=3
set default=0

# Specify boot order to prioritize CD-ROM
set root='(cd0)'

menuentry "OpenOS" {
    multiboot /boot/openos.bin
    boot
}
